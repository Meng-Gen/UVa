#include <iostream>
#include <string>
#include <stdio.h>

int query[5001][2000] = {};

int main(int argc, char* argv[])
{
    std::string input_string;
    int case_id = 1;
    while (std::cin >> input_string)
    {
        std::cout <<

        case_id++;
    }

    return 0;
}